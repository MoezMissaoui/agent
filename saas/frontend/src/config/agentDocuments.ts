/**
 * Max number of knowledge files per agent enforced in the UI only (no DB change).
 * Increase later when multi-file upload is implemented end-to-end.
 */
export const MAX_DOCUMENTS_PER_AGENT = 1;
